server:
  port: 8010
spring:
  application:
    name: microservice-consumer-movie
eureka:
  client:
    service-url:
      defaultZone: http://user:passwork123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true